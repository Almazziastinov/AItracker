# Notemind Backend

Интеллектуальный AI-ассистент для планирования задач и событий, интегрированный с мессенджером MAX.

## 🚀 Быстрый старт

Этот гайд поможет вам развернуть и запустить бэкенд проекта локально с помощью Docker.

### 1. Предварительные требования

-   **Docker и Docker Compose:** Убедитесь, что на вашей машине установлен Docker. [Инструкция по установке](https://docs.docker.com/get-docker/).
-   **Node.js и npm:** Необходимы для запуска туннеля. [Инструкция по установке](https://nodejs.org/en/download/).
-   **Git:** Для клонирования репозитория.

### 2. Клонирование репозитория

```bash
git clone <URL вашего репозитория>
cd notemind_backend
```

### 3. Настройка окружения (`.env`)

Создайте файл `.env` в корневой папке проекта. Он необходим для хранения всех секретных ключей и токенов.

```bash
touch .env
```

Откройте файл `.env` и добавьте в него следующие переменные:

```env
# Токен для авторизации в GigaChat API
GIGACHAT_CREDENTIALS="ВАШ_ТОКЕН_GIGACHAT"

# Токен вашего бота в платформе MAX
MAX_BOT_TOKEN="ВАШ_ТОКЕН_MAX_BOT"

# Ключ для API сервиса OpenRouteService (для геокодинга)
ORS_API_KEY="ВАШ_КЛЮЧ_ORS"

# URL для вебхука (этот адрес мы получим на следующем шаге)
WEBHOOK_URL=""
```
**Важно:** Не коммитьте `.env` файл в систему контроля версий.

### 4. Создание туннеля с помощью `localtunnel`

Поскольку для работы вебхуков от MAX API требуется публичный HTTPS-адрес, мы создадим туннель до нашего локального сервера, который будет работать на порту 8000.

1.  **Установите `localtunnel` глобально:**
    ```bash
    npm install -g localtunnel
    ```

2.  **Запустите туннель для порта 8000:**
    ```bash
    lt --port 8000
    ```

3.  В терминале вы увидите сообщение вида: `your url is: https://some-random-name.loca.lt`. **Скопируйте этот URL.**

### 5. Обновление `.env` файла

Вернитесь в ваш `.env` файл и вставьте скопированный URL в переменную `WEBHOOK_URL`.

```env
# ... (остальные переменные)
WEBHOOK_URL="https://some-random-name.loca.lt"
```

### 6. Сборка и запуск Docker-контейнеров

Теперь, когда все настроено, соберите и запустите Docker-контейнеры. Это создаст изолированное окружение для приложения и базы данных.

```bash
docker-compose up --build -d
```
-   `--build`: принудительно пересобирает образ, подхватывая все последние изменения.
-   `-d`: запускает контейнеры в фоновом (detached) режиме.

### 7. Регистрация вебхука

Последний шаг — сообщить платформе MAX наш новый публичный URL для получения сообщений.

Выполните следующую команду:
```bash
docker-compose exec app python set_webhook.py
```
Вы должны увидеть сообщение об успешной установке вебхука.

### 🎉 Готово!

Ваш бэкенд запущен и готов к работе. Вы можете начинать общаться с ботом в мессенджере MAX.

## ⚙️ Основные команды

-   **Запустить проект:** `docker-compose up --build -d`
-   **Остановить проект:** `docker-compose down`
-   **Посмотреть логи приложения:** `docker-compose logs -f app`
-   **Перезапустить только контейнер приложения:** `docker-compose restart app`

##  troubleshooting

-   **Бот не отвечает:** Скорее всего, ваш туннель (`localtunnel`) перезапустился и сменил URL. Повторите шаги 4, 5 и 7, чтобы обновить адрес и зарегистрировать новый вебхук.
-   **Ошибка `NameError` или `ModuleNotFoundError` при запуске:** Убедитесь, что вы выполнили `docker-compose up --build`, чтобы все зависимости и файлы были корректно установлены и скопированы в контейнер.

